#include <stdio.h>

/**
 * main- This program prints the upper case and lower case of alphabets
 * Return: Always (0) Success
 */

int main(void)
{
	char l;
	char u;

	for (l = 'a'; l <= 'z'; l++)
	{
		putchar(l);
	}
	for (u + 'A'; u <= 'Z'; u++)
	{
		putchar(u);
	}
	return (0);
}
